# --- segredos (NUNCA versionar) ---------------------------------------------
.env
.env.*
!.env.example
*.pem
*.key
secrets/

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.eggs/
.installed.cfg
pip-wheel-metadata/

# --- ambientes ---------------------------------------------------------------
.venv/
venv/
env/
ENV/

# --- testes / qualidade ------------------------------------------------------
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
coverage.xml
junit*.xml

# --- dados locais ------------------------------------------------------------
# Ancorados na raiz com "/" de proposito: um padrao solto como `media/` casa em
# QUALQUER profundidade e ja engoliu o pacote src/lukato/adapters/media/ inteiro
# — 8 modulos que existiam em disco, passavam nos testes localmente e nunca
# chegaram ao repositorio. So a CI acusou, com ModuleNotFoundError.
*.db
*.sqlite
*.sqlite3
lukato.db*
/var/
/data/
/media/
/tmp/
/logs/
*.log
# Videos locais que o docker-compose monta em /app/video — conteudo do usuario,
# nunca do repositorio.
/video/

# --- editores / SO -----------------------------------------------------------
.idea/
.vscode/
*.swp
.DS_Store
Thumbs.db
